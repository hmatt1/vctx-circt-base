# Prebuilt CIRCT + Python 3.14t image (see .github/workflows/docker-base.yml).
# Forks: set BASE_IMAGE to ghcr.io/<you>/<repo>-circt-base:main or build docker/base locally.
ARG BASE_IMAGE=ghcr.io/hmatt1/vctx-circt-base:main
FROM ${BASE_IMAGE}
